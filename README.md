# -Simple-User-Management-CRUD-App-Task4-Avinash-Pal
A lightweight full‑stack CRUD app that lets users view, add, and delete user records via a Node / Express backend API (with CORS and JSON handling) and a plain HTML/CSS/JavaScript frontend that consumes the API using
